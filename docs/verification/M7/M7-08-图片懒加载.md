# M7-08 - 图片懒加载 - 验证文档

> 对应任务: [01-迭代计划.md](../plan/01-迭代计划.md#任务-M7-08)
> 涉及 Spec: [12-08-图片懒加载规格.md](../spec/12-博客增强模块/12-08-图片懒加载规格.md)

## 验证目标

验证图片懒加载功能：Intersection Observer API、实现 v-lazyload 指令、Markdown 图片支持。

## 交付物检查

| 交付物 | 路径 | 存在 |
|--------|------|------|
| lazyload.ts 指令 | frontend/src/directives/lazyload.ts | ✅ |
| v-lazyload 指令注册 | frontend/src/main.ts | ✅ |
| PostCard.vue 懒加载 | frontend/src/components/blog/PostCard.vue | ✅ |
| 懒加载 CSS 样式 | frontend/src/assets/styles/main.css | ✅ |

## 测试用例

### TC-01: 前端编译检查

**执行命令**:
```bash
cd frontend && npm run build
```

**预期结果**:
- 退出码 = 0
- 无 TypeScript 错误

### TC-02: lazyload.ts 文件检查

**执行命令**:
```bash
grep -n "IntersectionObserver\|lazy\|observe" frontend/src/directives/lazyload.ts
```

**预期结果**:
- 找到 IntersectionObserver 实现
- 找到懒加载逻辑

### TC-03: 指令注册检查

**执行命令**:
```bash
grep "lazyload" frontend/src/main.ts
```

**预期结果**:
- 找到 v-lazyload 指令注册

## 验证报告（由 AI 填写）

**验证时间**：{YYYY-MM-DD HH:mm:ss}
**验证结果**：✅ PASSED / ❌ FAILED

### TC-01: 前端编译检查
- **命令**: `cd frontend && npm run build`
- **退出码**: {0|非0}
- **运行时间**: {Xs}
- **状态**: ✅ PASSED / ❌ FAILED
- **输出摘要**: {命令输出的关键部分}

### TC-02: lazyload.ts 文件检查
- **命令**: `grep -n "IntersectionObserver\|lazy\|observe" frontend/src/directives/lazyload.ts`
- **退出码**: {0|非0}
- **运行时间**: {Xs}
- **状态**: ✅ PASSED / ❌ FAILED
- **输出摘要**: {命令输出的关键部分}

### TC-03: 指令注册检查
- **命令**: `grep "lazyload" frontend/src/main.ts`
- **退出码**: {0|非0}
- **运行时间**: {Xs}
- **状态**: ✅ PASSED / ❌ FAILED
- **输出摘要**: {命令输出的关键部分}

## 验收标准

- [ ] TC-01 前端编译检查通过
- [ ] TC-02 lazyload.ts 文件检查通过
- [ ] TC-03 指令注册检查通过
- [ ] IntersectionObserver 已实现
- [ ] v-lazyload 指令已注册
- [ ] PostCard 图片已使用懒加载
- [ ] 所有测试通过

## 注意事项

- **执行命令是必须的**：每个测试用例必须包含可执行的命令
- **不能只做静态检查**：读取代码检查结构不算验证，必须实际运行
- **失败时提供实际输出**：错误信息应包含实际命令的输出，便于调试
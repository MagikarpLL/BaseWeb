# 07-部署配置

## 7.1 Docker 配置

### docker-compose.yml

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: personal_website_db
    environment:
      POSTGRES_DB: personal_website
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
```

## 7.2 环境变量

### 后端 (application.yml)

```yaml
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:personal_website}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:password}

  security:
    jwt:
      secret: ${JWT_SECRET:your-256-bit-secret-key-here}
      access-token-expiration: 900000    # 15 分钟
      refresh-token-expiration: 604800000 # 7 天
```

### 前端 (.env)

```bash
# 开发环境
VITE_API_BASE_URL=http://localhost:8080/api
VITE_SITE_URL=http://localhost:5173

# 生产环境
# VITE_API_BASE_URL=https://api.yoursite.com/api
# VITE_SITE_URL=https://yoursite.com
```

### Docker 环境变量

```bash
# .env
DB_PASSWORD=your_secure_password_here
```

## 7.3 Nginx 配置

```nginx
server {
    listen 80;
    server_name yoursite.com;

    # 前端静态资源
    location / {
        root /var/www/frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    # 后端 API 代理
    location /api/ {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 上传文件
    location /uploads/ {
        alias /var/www/uploads/;
    }

    # Gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript;
}
```

## 7.4 埋点实现

### 前端埋点

```typescript
// 页面访问时
function trackPageView(url: string) {
  fetch('/api/analytics/track', {
    method: 'POST',
    body: JSON.stringify({
      type: 'pageview',
      url: window.location.pathname,
      referrer: document.referrer,
      visitorId: getVisitorId(),
    })
  })
}

// 生成访客 ID
function getVisitorId() {
  let visitorId = getCookie('visitor_id')
  if (!visitorId) {
    visitorId = generateUUID()
    setCookie('visitor_id', visitorId, 365) // 1 年有效期
  }
  return visitorId
}
```

### Vue Router 监听

```typescript
router.afterEach((to) => {
  trackPageView(to.fullPath)
})
```

### 性能考虑

- 埋点请求异步发送，不阻塞页面
- 使用 requestIdleCallback 批量发送
- 数据先存本地，隔段时间批量上报

## 7.5 统计图表

```bash
npm install echarts vue-echarts
```

```vue
<template>
  <v-chart :option="chartOption" />
</template>

<script setup>
const chartOption = {
  xAxis: { type: 'category', data: ['4/14', '4/15', '4/16', '4/17', '4/18', '4/19', '4/20'] },
  yAxis: { type: 'value' },
  series: [
    { name: 'PV', type: 'line', data: [1200, 1100, 1150, 1300, 1200, 1089, 1234] },
    { name: 'UV', type: 'line', data: [400, 380, 390, 420, 400, 398, 456] }
  ]
}
</script>
```

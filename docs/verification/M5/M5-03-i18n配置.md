# M5-03 - i18n 配置 + useLocale composable - 验证文档

> 对应任务: [01-迭代计划.md](../plan/01-迭代计划.md#任务-M5-03)
> 涉及 Spec: [11-01-语言切换功能-spec.md](../spec/11-国际化模块/11-01-语言切换功能-spec.md)

## 验证目标

验证 i18n 实例正确配置，useLocale composable 提供完整的语言切换功能。

## 交付物检查

| 交付物 | 路径 | 存在 |
|--------|------|------|
| i18n 配置文件 | frontend/src/i18n/index.ts | ✅ |
| useLocale composable | frontend/src/composables/useLocale.ts | ✅ |

## 测试用例

### TC-01: 前端编译检查

**执行命令**:
```bash
cd frontend && npm run build
```

**预期结果**:
- 退出码 = 0
- 输出包含 `dist/` 目录生成成功标志
- 无 TypeScript 错误

**验证方式**:
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 执行 `npm run build` | 命令执行完成 |
| 2 | 检查退出码 | 退出码 = 0 |
| 3 | 检查 dist 目录 | dist/index.html 存在 |

### TC-02: useLocale 单元测试

**执行命令**:
```bash
cd frontend && npm run test:unit -- --run tests/unit/useLocale.test.ts
```

**预期结果**:
- 退出码 = 0
- 输出包含测试通过数量
- 无测试失败

**验证方式**:
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 执行单元测试 | 测试执行完成 |
| 2 | 检查退出码 | 退出码 = 0 |
| 3 | 检查输出 | 3 passed, 0 failed |

### TC-03: 语言切换 E2E 测试

**执行命令**:
```bash
cd frontend && npm run test:e2e -- --grep "LanguageSwitch"
```

**前置条件**:
- 前端开发服务器已启动或使用内置 webServer

**预期结果**:
- 退出码 = 0
- 输出包含测试通过数量
- E2E 测试流程通过

**验证方式**:
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 启动开发服务器 | 服务器就绪 |
| 2 | 执行 E2E 测试 | 测试执行完成 |
| 3 | 检查退出码 | 退出码 = 0 |
| 4 | 检查输出 | 3 passed, 0 failed |

## 验证报告（由 AI 填写）

**验证时间**：{YYYY-MM-DD HH:mm:ss}
**验证结果**：✅ PASSED / ❌ FAILED

### TC-01: 前端编译检查
- **命令**: `cd frontend && npm run build`
- **退出码**: {0|非0}
- **运行时间**: {Xs}
- **状态**: ✅ PASSED / ❌ FAILED
- **输出摘要**: {命令输出的关键部分}

### TC-02: useLocale 单元测试
- **命令**: `cd frontend && npm run test:unit -- --run tests/unit/useLocale.test.ts`
- **退出码**: {0|非0}
- **运行时间**: {Xs}
- **状态**: ✅ PASSED / ❌ FAILED
- **输出摘要**: {命令输出的关键部分}

### TC-03: 语言切换 E2E 测试
- **命令**: `cd frontend && npm run test:e2e -- --grep "LanguageSwitch"`
- **退出码**: {0|非0}
- **运行时间**: {Xs}
- **状态**: ✅ PASSED / ❌ FAILED
- **输出摘要**: {命令输出的关键部分}

## 验收标准

- [ ] TC-01 前端编译检查通过
- [ ] TC-02 单元测试通过
- [ ] TC-03 E2E 测试通过
- [ ] 所有测试通过

## 注意事项

- **执行命令是必须的**：每个测试用例必须包含可执行的命令
- **不能只做静态检查**：读取代码检查结构不算验证，必须实际运行
- **失败时提供实际输出**：错误信息应包含实际命令的输出，便于调试